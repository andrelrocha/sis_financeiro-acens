const path = require("path");

module.exports = {
  config: path.resolve("src", "config", "config.json"),
  "models-path": path.resolve("src", "models"),
  "seeders-path": path.resolve("src", "dbEssentials", "seeders"),
  "migrations-path": path.resolve("src", "dbEssentials", "migrations"),
};